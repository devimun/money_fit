# Historical learning notes

These notes capture decisions and implementation snapshots made while
refactoring MoneyFit. They are historical context, not the current public API
contract. Current ownership and migration status live in the root README,
`docs/adr/`, and `docs/update/refactor-architecture/`.
