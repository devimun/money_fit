# Provide deployment credentials at execution time; never commit a workstation
# path or service-account JSON file.
json_key_file(ENV.fetch("GOOGLE_PLAY_SERVICE_ACCOUNT_JSON"))
package_name(ENV.fetch("ANDROID_APPLICATION_ID", "com.moneyfitapp.app"))
